## Procedure to create the virtualenv on Unix / MAC envirorment

 virtualenv env --python=3.11
### activate the virtual env (on Linux / Mac)
  source ./env/bin/activate
### Install the requirements on the virtualenv
  pip install -r requirements.txt 

#### Execute the script

python geo_discover.py

